int main(void)
{
    HAL_Init();
    SystemClock_Config();
    MX_GPIO_Init();
    MX_TIM5_Init();

    HAL_TIM_OC_Start(&htim5, TIM_CHANNEL_1);

    while(1)
    {
        // Toggle output on compare automatically
    }
}
