int main(void)
{
    HAL_Init();
    SystemClock_Config();
    MX_GPIO_Init();
    MX_TIM4_Init();

    HAL_TIM_OC_Start(&htim4, TIM_CHANNEL_1);

    while(1)
    {
        // No need to handle in main, hardware toggles pin
    }
}
