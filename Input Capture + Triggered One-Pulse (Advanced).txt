int main(void)
{
    HAL_Init();
    SystemClock_Config();
    MX_GPIO_Init();
    MX_TIM3_Init();

    HAL_TIM_OnePulse_Start_IT(&htim3, TIM_CHANNEL_1);

    while(1)
    {
        // ISR handles pulse generation
    }
}
