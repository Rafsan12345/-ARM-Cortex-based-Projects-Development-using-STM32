int main(void)
{
    HAL_Init();
    SystemClock_Config();
    MX_GPIO_Init();
    MX_TIM2_Init();

    HAL_TIM_OnePulse_Start(&htim2, TIM_CHANNEL_1);

    while(1)
    {
        // Trigger external signal to start one pulse
    }
}
