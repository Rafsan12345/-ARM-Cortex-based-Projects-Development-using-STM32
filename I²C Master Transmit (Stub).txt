int main(void)
{
    HAL_Init();
    SystemClock_Config();
    MX_GPIO_Init();
    MX_I2C1_Init();

    uint8_t data = 0x55;

    while(1)
    {
        HAL_I2C_Master_Transmit(&hi2c1, (0x50<<1), &data, 1, HAL_MAX_DELAY);
        HAL_Delay(500);
    }
}
